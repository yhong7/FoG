potential_triplet_prompt = """
    # 任务
你将收到一个「问题」以及一组以三元组形式给出的「知识」：(主语, 关系, 宾语)。
请只基于提供的知识完成推理，不要引入外部常识或编造信息。

# 输出格式（JSON）
严格输出一个 JSON列表，对象包含从知识中挑出**可能用于回答问题**的三元组。
要求输出不少于5条三元组。

# 严格约束
1. 只能使用输入中的三元组；不要改写或新造三元组、不要添加外部事实。
2. 只要任何一条三元组与问题有任何的关联可能性，则将其保留。由于任务需求，输出的子图要求很宽泛，任何可能连接到答案的的三元组都需要保留。
3. 输出三元组**必须逐字保持**输入原文格式 (主语, "关系", 宾语)。
4. 输出中只包含一个 JSON 列表，用```json ```包裹，不要添加说明文字、注释或多余空格/换行以外的内容。

# 示例输入
问题：<在此粘贴问题>
知识：[
[主语, "关系", 宾语],
...
]

# 示例输出（结构示例）
```json
[
    ["主语A","关系","宾语A"],
    ["主语B","关系","宾语B"]
]
}}
```

# 下面是真实输入：
问题：{query}
知识：{triplets}
"""


get_answer_prompt = """
你将收到一个「问题」以及一组以三元组形式给出的「知识」：(主语, 关系, 宾语)。
请基于提供的知识完成推理，不要引入外部信息。

# 输出格式（JSON）
严格输出一个 JSON，对象包含两个键：
- "predict_answer": List[str]. 若仅凭给定知识仍无法确定答案，填 []；若已能确定，则填写三元组中的原始实体的列表，最前的索引代表最可能的答案（禁止做翻译等任何冗余操作，输出原始实体）。
- "subsequent_question": List[str]: 若 "predict_answer" 仍为空，为了得到答案你最想追问的**1–3 个**澄清问题；若已能回答，输出空数组 []。

# 严格约束
1. 只能使用输入中的实体与三元组；不要改写或新造三元组、不要使用外部知识，答案只允许是提供的若干三元组中的实体之一，禁止修饰和描述。
2. 如信息缺失导致无法得出确定结论，应将 "predict_answer" 置空，并在 "subsequent_question" 中提出**为得到答案所缺的关键信息**（例如地点、时间、隶属关系等）。
3. 如果凭现有的知识已经能给出可能的答案：
   - 给出 predict_answer;
   - "subsequent_question" 置为 []。
4. 输出中只包含 JSON，用```json ```包裹，不要添加说明文字、注释或多余空格/换行以外的内容。
6. 所有的实体id，如“m.04st6lz”，不可能是答案，需要进一步搜索。

# 示例输出1（无法给出答案）
```json
{{
  "predict_answer": [],
  "subsequent_question": [
    "为得到答案需要补充的关键问题1？"
  ]
}}
```

# 示例输出2（可以给出答案）
```json
{{
  "predict_answer": ["answer1", "answer2"],
  "subsequent_question": []
}}
```

# 下面是真实输入：
问题：{query}
知识：{triplets}
"""

get_answer_prompt_with_entity_response = """
你将收到一个问题以及一组以三元组形式给出的知识。
请基于提供的知识完成推理，不要引入外部信息。

# 输出格式（JSON）
严格输出一个 JSON，对象包含三个键：
- "predict_answer": List[str]. 若仅凭给定知识仍无法确定答案，填 []；若已能确定，则填写三元组中的原始实体的列表（至少5个候选答案），最前的索引代表最可能的答案（禁止做翻译等任何冗余操作，输出原始实体）。
- "subsequent_question": List[str]: 若 "predict_answer" 仍为空，为了得到答案你最想追问的**1–3 个**澄清问题；若已能回答，输出空数组 []。
- "confused_entities": 尚不明确是否是答案的实体，如可能有未知的约束但还没被搜索到，或该实体为搜索路径中的中间实体。

# 严格约束
1. 只能使用输入中的实体与三元组；不要改写或新造三元组、不要使用外部知识，答案只允许是提供的若干三元组中的实体之一，禁止修饰和描述。
2. 如信息缺失导致无法得出确定结论，应将 "predict_answer" 置空，并在 "subsequent_question" 中提出**为得到答案所缺的关键信息**（例如地点、时间、隶属关系等）。
3. 如果凭现有的知识已经能给出可能的答案：
   - 给出 predict_answer;
   - "subsequent_question" 置为 []。
4. 输出中只包含 JSON，用```json ```包裹，不要添加说明文字、注释或多余空格/换行以外的内容。
6. 所有的实体id，如“m.04st6lz”，不可能是答案，需要进一步搜索。

# 示例输出1（无法给出答案）
```json
{{
  "predict_answer": [],
  "subsequent_question": [
    "为得到答案需要补充的关键问题1？"
  ],
  "confused_entities": ["e1", ...]
}}
```

# 示例输出2（可以给出答案）
```json
{{
  "predict_answer": ["answer1", "answer2"],
  "subsequent_question": [],
  "confused_entities": []
}}
```

# 下面是真实输入：
问题：{query}
知识：{triplets}
"""

get_final_answer_prompt = """
    # 任务
你将收到一个「问题」以及一组以三元组形式给出的「知识」：(主语, 关系, 宾语)。
请基于提供的知识完成推理。

# 输出格式（JSON）
严格输出一个 JSON，对象包含一个键：
- "predict_answer": List[str]. 填写三元组中的原始实体，只输出满足要求的答案。

# 严格约束
1. 只能使用输入中的实体与三元组；不要改写或新造三元组、不要使用外部知识，答案只允许是提供的若干三元组中的实体之一，禁止修饰和描述。
2. 输出不能是一个id（如m.1234），而是需要是一个有意义的实体。
3. 至少提供一个答案。

# 示例输出
```json
{{
  "predict_answer": ["answer1", "answer2", ...],
}}
```

# 下面是真实输入：
问题：{query}
知识：{triplets}
"""

filter_relations_prompt = """
你会收到一个问题与一系列可能的relations，你需要过滤出那些确信与问题有可能有关的三元组。
注意，问题是一个复杂多跳问题，但凡某个relation与问题有一点可能性，则需要保留。
注意，形如“m.abc123"的节点表示了一个事实，隐含部分元信息，如人口数、年份等。如果有必要，进行保留。

输出格式：
```json
{{
    "useful_relations": ["r1", "r2", ...]
}}

下面是真实输入：
问题：{query}
全部的relations: {relations}
"""


get_final_unique_answer_prompt = """
# 任务
你将收到一个「问题」以及一组以三元组形式给出的「知识」：(主语, 关系, 宾语)。
请基于提供的知识完成推理。

# 输出格式（JSON）
严格输出一个 JSON，对象包含一个键：
- "predict_answer": List[str]. 填写三元组中的原始实体，里面仅包含一个字符串，表示推理后得到的最可能的答案。

# 严格约束
1. 只能使用输入中的实体与三元组；不要改写或新造三元组、不要使用外部知识，答案只允许是提供的若干三元组中的实体之一，禁止修饰和描述。
2. 输出不能是一个id（如m.1234），而是需要是一个有意义的实体。

# 示例输出
```json
{{
  "predict_answer": ["answer"],
}}
```

# 下面是真实输入：
问题：{query}
知识：{triplets}
"""


find_useful_relations_prompt = """
# 输入
问题：{query}
待判定关系：{relations}

# 输出
请输出一个关系列表，如果relation与问题有可能相关，就进行保留。否则进行剔除。
    
# 示例输出格式
```json
{{
  "useful_relations": ["rel1", "rel2", ...],
}}
```

"""

